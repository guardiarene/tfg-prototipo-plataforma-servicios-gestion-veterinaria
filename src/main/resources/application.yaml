spring:
  profiles:
    active: dev
  application:
    name: psygcs

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org:
      hibernate:
        SQL: INFO

server:
  port: 8080
